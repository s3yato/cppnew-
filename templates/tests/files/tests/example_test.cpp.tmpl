#include <gtest/gtest.h>

TEST(ExampleTest, BasicAssertions) {
    EXPECT_EQ(1 + 1, 2);
    EXPECT_TRUE(true);
}

TEST(ExampleTest, StringComparison) {
    std::string a = "hello";
    std::string b = "hello";
    EXPECT_EQ(a, b);
}